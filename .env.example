# Copy to .env and fill in. Never commit the real .env.

# Used by deploy.sh to know where to ssh/push to.
VPS_HOST=ubuntu@51.81.80.126

# Required by the API server (see CLAUDE.md).
DATABASE_URL=file:./data/mini_golf.db
PORT=5761
APP_PASSWORD=changeme

# Optional. Comma-separated. Defaults to allowing any origin if unset.
# CORS_ORIGIN=https://golf.flyboybyte.com
